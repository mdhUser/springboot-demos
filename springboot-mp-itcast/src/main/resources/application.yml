server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mp_db?useSSL=false&autoReconnect=true&characterEncoding=utf8&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
#  mp配置
mybatis-plus:
  type-aliases-package: org.maxwell.mp.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
